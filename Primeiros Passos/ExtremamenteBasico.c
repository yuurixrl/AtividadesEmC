#include <stdio.h>

int main() {
    int A, B, X;

    // Leitura dos valores inteiros
    scanf("%d", &A);
    scanf("%d", &B);

    // Cálculo da soma
    X = A + B;

    // Impressão do resultado conforme o formato especificado
    printf("X = %d\n", X);

    return 0;
}